class DataAnalyzer{static validateInput(a){if(!Array.isArray(a))throw Error("Input must be an array of transactions");if(0===a.length)throw Error("Input array cannot be empty");let t=["Datum","Zeit","Filiale","Transaktionsnummer","Artikel","Menge","Umsatz","Aktion"].filter(t=>!a[0].hasOwnProperty(t));if(t.length>0)throw Error(`Missing required fields: ${t.join(", ")}`)}static analyzeData(a){this.validateInput(a);let t=TimeAnalyzer.analyze(a),e=SpendingAnalyzer.analyze(a),n=ProductAnalyzer.analyze(a);return{...t,...e,...n}}}
